<form method="post" action="{{ path('app_processus_delete', {'id': processu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ processu.id) }}">
    <button class="btn">Delete</button>
</form>
